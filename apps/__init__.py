"""Top-level namespace for deployment-shape shells (e.g. apps.desktop).

Never imported by `src/orchai` -- the dependency direction is one-way,
shells depend on the backend, never the reverse (ADR-017).
"""
