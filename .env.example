# Local default. Override when you want a different SQLite path.
ORCHAI_DATABASE_URL=sqlite:///.orchai/orchai.db

# PostgreSQL example. Secrets should come from environment variables or a
# secret manager, not committed project configuration.
# ORCHAI_DATABASE_URL=postgresql://postgres:change-me@localhost:5432/orchai
